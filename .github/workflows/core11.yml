name: core 11

on: [ push, pull_request ]

jobs:
  tests:
    name: v11
    runs-on: ubuntu-20.04
    strategy:
      # This prevents cancellation of matrix job runs, if one/two already failed and let the
      # rest matrix jobs be executed anyway.
      fail-fast: false
      matrix:
        php: [ '7.4', '8.1', '8.2' ]
        composerInstall: [  'composerInstallHighest' ]
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Validate composer.json and composer.lock
        run: Build/Scripts/runTests.sh -t 11 -p ${{ matrix.php }} -s composer -e 'validate'

      - name: Install testing system
        run: Build/Scripts/runTests.sh -t 11 -p ${{ matrix.php }} -s ${{ matrix.composerInstall }}

      - name: Lint PHP
        run: Build/Scripts/runTests.sh -t 11 -p ${{ matrix.php }} -s lint

      - name: Functional Tests
        run: Build/Scripts/runTests.sh -t 11 -p ${{ matrix.php }} -s functional

      - name: Unit Tests
        run: Build/Scripts/runTests.sh -t 11 -p ${{ matrix.php }} -s unit

      - name: Backend Functional Demo Tests
        run: Build/Scripts/runTests.sh -t 11 -p ${{ matrix.php }} -s acceptanceInstall
